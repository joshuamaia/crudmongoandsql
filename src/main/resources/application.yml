spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/products
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    mongodb:
      host: localhost
      port: 27017
      database: products
